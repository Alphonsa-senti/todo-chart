apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-calculator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "java-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-calculator.selectorLabels" . | nindent 4 }}
